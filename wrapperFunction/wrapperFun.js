const names = "nabin";
console.log(names);

// !in the compilation process given code will wrape on anonymous function which is Immediately Invoke function Expression.
// ? which make the given code is private 
// (function () {
//     const names = "nabin";
//     console.log(names);
// });